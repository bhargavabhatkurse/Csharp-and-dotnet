// // See https://aka.ms/new-console-template for more information
// using System.Net.Mime;

// Console.WriteLine("Hello, World!");

// // string n = "100";
// // int x = Int32.Parse(n);
// // Console.WriteLine(x);

// //define an array
// int[] name =  [1,2,3];

// string[] cities = {"city1","city2"};

// foreach(var item in cities) {
//     Console.WriteLine(item);
// }

// // var keyword :- instructs the compiler to infer the type of the variable from the expression on the right side of the initialization statement

// // var query = from cities in 

// // cities.Where(city => cities.Contains('a'));

// int[] numbers = new int[7] { 0, 1, 2, 3, 4, 5, 6 };

//         // 2. Query creation.
//         // numQuery is an IEnumerable<int>
//         var numQuery =
//             from num in numbers
//             where (num % 2) == 0
//             select num;

//   foreach (int num in numQuery)
//         {
//             Console.Write("{0,5} ", num);
//         }

// // {0} refers to the argument index, indicating that the value to be printed will come from the first argument after the format string.
// // ,1 is setting the width of the field where the number will be printed. In this case, it's setting a field width of 1 character for each number.


